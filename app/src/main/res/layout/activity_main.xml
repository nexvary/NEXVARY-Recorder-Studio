<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/navy"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp"
        android:layoutDirection="rtl">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="NEXVARY Recorder Studio"
            android:textColor="@color/white"
            android:textSize="26sp"
            android:textStyle="bold"
            android:gravity="end" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="تسجيل شاشة وصوت احترافي + استبدال الصوت + بث ملف فيديو عبر RTMPS"
            android:textColor="@color/silver"
            android:textSize="15sp"
            android:gravity="end" />

        <Button android:id="@+id/btnScreen" style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="match_parent" android:layout_height="64dp" android:layout_marginTop="28dp"
            android:text="تسجيل الشاشة" />
        <Button android:id="@+id/btnVoice" style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="match_parent" android:layout_height="64dp" android:layout_marginTop="12dp"
            android:text="استوديو الصوت المحسن" />
        <Button android:id="@+id/btnReplace" style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="match_parent" android:layout_height="64dp" android:layout_marginTop="12dp"
            android:text="إضافة / استبدال الصوت في فيديو" />
        <Button android:id="@+id/btnLive" style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="match_parent" android:layout_height="64dp" android:layout_marginTop="12dp"
            android:text="بث فيديو مسجل مسبقًا كـ Live" />

        <TextView
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:text="كل المعالجة محلية على الهاتف. لا يتم رفع ملفاتك إلى خادم NEXVARY."
            android:textColor="@color/green" android:gravity="end" android:textSize="13sp" />
    </LinearLayout>
</ScrollView>
